---
phase: 02-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/themes.xml
  - app/src/main/res/values-night/themes.xml
  - app/src/main/AndroidManifest.xml
  - app/src/main/kotlin/com/acalc/ui/AppTheme.kt
  - app/src/main/kotlin/com/acalc/ui/MainActivity.kt
autonomous: true
requirements: [APP-01]
must_haves:
  truths:
    - "App launches without crashing — MainActivity renders a Compose surface"
    - "Material You dynamic color is applied on API 31+ devices"
    - "Fallback purple color scheme is used on API 26-30 devices"
    - "Edge-to-edge display is enabled — system bars are transparent"
  artifacts:
    - path: "app/src/main/res/values/themes.xml"
      provides: "XML theme stub for AAPT resource linking"
      contains: "Theme.Acalc"
    - path: "app/src/main/kotlin/com/acalc/ui/AppTheme.kt"
      provides: "AcalcTheme composable with dynamic color + fallback"
      exports: ["AcalcTheme"]
    - path: "app/src/main/kotlin/com/acalc/ui/MainActivity.kt"
      provides: "Activity entry point with edge-to-edge and Compose setContent"
      contains: "enableEdgeToEdge"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "MainActivity declaration with launcher intent-filter"
      contains: "android.intent.action.MAIN"
  key_links:
    - from: "AndroidManifest.xml"
      to: "res/values/themes.xml"
      via: "android:theme=\"@style/Theme.Acalc\""
      pattern: "android:theme.*Theme\\.Acalc"
    - from: "MainActivity.kt"
      to: "AppTheme.kt"
      via: "AcalcTheme { } wrapping setContent"
      pattern: "AcalcTheme"
---

<objective>
Create the Android app infrastructure: XML theme stub (resolving the Phase 1 AAPT deferral), Material You dynamic theming composable with purple fallback, MainActivity with edge-to-edge display, and updated AndroidManifest.

Purpose: Establish the launchable app entry point with correct theming so Plan 02 can add navigation and placeholder screens on top.
Output: App that launches, renders a blank Compose surface with Material 3 theming applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/phases/02-app-shell/02-RESEARCH.md

@app/src/main/AndroidManifest.xml
@app/build.gradle.kts
@gradle/libs.versions.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XML theme stubs and update AndroidManifest</name>
  <files>
    app/src/main/res/values/themes.xml,
    app/src/main/res/values-night/themes.xml,
    app/src/main/AndroidManifest.xml
  </files>
  <read_first>
    app/src/main/AndroidManifest.xml,
    .planning/phases/02-app-shell/02-RESEARCH.md
  </read_first>
  <action>
Create three files/modifications:

1. Create `app/src/main/res/values/themes.xml` (per D-04, resolving Phase 1 AAPT deferral):
```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Acalc" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
```

2. Create `app/src/main/res/values-night/themes.xml` (dark mode variant, same parent):
```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Acalc" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
```

3. Update `app/src/main/AndroidManifest.xml` to declare MainActivity with launcher intent-filter. The final manifest must be:
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="ACALC"
        android:theme="@style/Theme.Acalc"
        android:supportsRtl="true">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

Key details:
- `android:theme="@style/Theme.Acalc"` references the XML style — this is what caused the AAPT failure in Phase 1 and was deferred
- `android:name=".ui.MainActivity"` uses the `.ui` sub-package (matching the `com.acalc.ui` package convention)
- `android:exported="true"` is required for launcher activities on Android 12+ (API 31)
- `android:windowSoftInputMode="adjustResize"` ensures keyboard input adjusts layout properly (needed for Phase 3/4 input fields)
- Do NOT add any network permissions — app is fully offline
  </action>
  <verify>
    <automated>cd /home/sanya/Desktop/ACALC && grep -q 'Theme.Acalc' app/src/main/res/values/themes.xml && grep -q 'Theme.Material3.DayNight.NoActionBar' app/src/main/res/values/themes.xml && grep -q 'android:theme="@style/Theme.Acalc"' app/src/main/AndroidManifest.xml && grep -q '.ui.MainActivity' app/src/main/AndroidManifest.xml && grep -q 'android:exported="true"' app/src/main/AndroidManifest.xml && grep -q 'android.intent.action.MAIN' app/src/main/AndroidManifest.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/src/main/res/values/themes.xml contains `name="Theme.Acalc"` and `parent="Theme.Material3.DayNight.NoActionBar"`
    - app/src/main/res/values-night/themes.xml contains `name="Theme.Acalc"` and `parent="Theme.Material3.DayNight.NoActionBar"`
    - AndroidManifest.xml contains `android:theme="@style/Theme.Acalc"` on the application element
    - AndroidManifest.xml contains `android:name=".ui.MainActivity"` with `android:exported="true"`
    - AndroidManifest.xml contains `android.intent.action.MAIN` and `android.intent.category.LAUNCHER` inside intent-filter
    - AndroidManifest.xml does NOT contain any `uses-permission` elements (no network permissions)
  </acceptance_criteria>
  <done>XML theme stubs exist for both light and dark, AndroidManifest declares MainActivity as launcher activity with Theme.Acalc applied</done>
</task>

<task type="auto">
  <name>Task 2: Create AcalcTheme composable and MainActivity with edge-to-edge</name>
  <files>
    app/src/main/kotlin/com/acalc/ui/AppTheme.kt,
    app/src/main/kotlin/com/acalc/ui/MainActivity.kt
  </files>
  <read_first>
    .planning/phases/02-app-shell/02-RESEARCH.md,
    app/build.gradle.kts
  </read_first>
  <action>
Create two Kotlin files in `com.acalc.ui` package:

1. Create `app/src/main/kotlin/com/acalc/ui/AppTheme.kt` (per D-04, D-05):
```kotlin
package com.acalc.ui

import android.os.Build
import androidx.compose.foundation.isSystemInDarkTheme
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.dynamicDarkColorScheme
import androidx.compose.material3.dynamicLightColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.platform.LocalContext

private val LightColorScheme = lightColorScheme(
    primary = Color(0xFF6650A4),
    secondary = Color(0xFF625B71),
    tertiary = Color(0xFF7D5260),
)

private val DarkColorScheme = darkColorScheme(
    primary = Color(0xFFD0BCFF),
    secondary = Color(0xFFCCC2DC),
    tertiary = Color(0xFFEFB8C8),
)

@Composable
fun AcalcTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    dynamicColor: Boolean = true,
    content: @Composable () -> Unit
) {
    val colorScheme = when {
        dynamicColor && Build.VERSION.SDK_INT >= Build.VERSION_CODES.S -> {
            val context = LocalContext.current
            if (darkTheme) dynamicDarkColorScheme(context)
            else dynamicLightColorScheme(context)
        }
        darkTheme -> DarkColorScheme
        else -> LightColorScheme
    }

    MaterialTheme(
        colorScheme = colorScheme,
        content = content
    )
}
```

Key details per D-04 and D-05:
- Dynamic color gated behind `Build.VERSION_CODES.S` (API 31) — prevents crash on API 26-30
- Fallback colors use M3 baseline purple (seed color is purple per D-05)
- `LightColorScheme` primary `0xFF6650A4` is Material 3 baseline purple
- `DarkColorScheme` primary `0xFFD0BCFF` is the M3 dark variant of baseline purple
- Function is public `AcalcTheme` — will be called from MainActivity and tests

2. Create `app/src/main/kotlin/com/acalc/ui/MainActivity.kt` (per D-06):
```kotlin
package com.acalc.ui

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            AcalcTheme {
                // AppShell will be added in Plan 02
            }
        }
    }
}
```

Key details per D-06:
- `enableEdgeToEdge()` is called BEFORE `setContent` — this is critical, calling it after causes incorrect system bar colors on the first frame
- `AcalcTheme` wraps all content — dynamic color applied to everything inside
- For now, the `AcalcTheme { }` body is empty — Plan 02 will add `AppShell()` inside it
- Import `enableEdgeToEdge` from `androidx.activity` (the extension function on ComponentActivity)

After creating both files, run: `cd /home/sanya/Desktop/ACALC && ./gradlew :app:assembleDebug` to verify the app compiles with the new theme and activity.
  </action>
  <verify>
    <automated>cd /home/sanya/Desktop/ACALC && ./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/src/main/kotlin/com/acalc/ui/AppTheme.kt exists with `package com.acalc.ui`
    - AppTheme.kt contains `fun AcalcTheme(` as a public composable
    - AppTheme.kt contains `Build.VERSION.SDK_INT >= Build.VERSION_CODES.S` guard for dynamic color
    - AppTheme.kt contains `dynamicDarkColorScheme` and `dynamicLightColorScheme` inside the S guard
    - AppTheme.kt contains `Color(0xFF6650A4)` as the light primary fallback (purple seed per D-05)
    - AppTheme.kt contains `MaterialTheme(` wrapping content
    - app/src/main/kotlin/com/acalc/ui/MainActivity.kt exists with `package com.acalc.ui`
    - MainActivity.kt contains `enableEdgeToEdge()` BEFORE `setContent`
    - MainActivity.kt contains `AcalcTheme {` inside setContent
    - MainActivity.kt extends `ComponentActivity()`
    - `./gradlew :app:assembleDebug` completes with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>AcalcTheme composable provides Material You dynamic color on API 31+ with purple fallback on older devices. MainActivity launches with edge-to-edge display and themed Compose content. Debug APK builds successfully.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` succeeds (no AAPT resource-linking errors, no compilation errors)
- `app/build/outputs/apk/debug/app-debug.apk` exists
- No network permissions in AndroidManifest.xml
- Theme XML stubs reference Theme.Material3.DayNight.NoActionBar
</verification>

<success_criteria>
- App compiles and produces a debug APK
- AndroidManifest declares MainActivity with launcher intent-filter and Theme.Acalc
- AcalcTheme composable handles dynamic color on API 31+ and falls back to purple on API 26-30
- enableEdgeToEdge() is called before setContent in MainActivity
- APP-01 infrastructure is complete (dynamic theming composable + XML theme stub)
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-01-SUMMARY.md`
</output>
