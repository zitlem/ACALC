---
phase: 02-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/kotlin/com/acalc/ui/AppShell.kt
  - app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt
  - app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt
  - app/src/main/kotlin/com/acalc/ui/MainActivity.kt
autonomous: true
requirements: [APP-02]
must_haves:
  truths:
    - "User can tap Calculator tab and see the Calculator placeholder screen"
    - "User can tap Converter tab and see the Converter placeholder screen"
    - "Active tab is visually indicated in the bottom navigation bar"
    - "Switching tabs does not cause a crash or navigation error"
  artifacts:
    - path: "app/src/main/kotlin/com/acalc/ui/AppShell.kt"
      provides: "Scaffold with NavigationBar and NavDisplay for two-tab navigation"
      exports: ["AppShell", "CalculatorRoute", "ConverterRoute"]
      min_lines: 40
    - path: "app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt"
      provides: "Calculator placeholder with centered icon and text"
      exports: ["CalculatorScreen"]
    - path: "app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt"
      provides: "Converter placeholder with centered icon and text"
      exports: ["ConverterScreen"]
  key_links:
    - from: "MainActivity.kt"
      to: "AppShell.kt"
      via: "AppShell() call inside AcalcTheme"
      pattern: "AppShell\\(\\)"
    - from: "AppShell.kt"
      to: "CalculatorScreen.kt"
      via: "entry<CalculatorRoute> in entryProvider"
      pattern: "entry<CalculatorRoute>"
    - from: "AppShell.kt"
      to: "ConverterScreen.kt"
      via: "entry<ConverterRoute> in entryProvider"
      pattern: "entry<ConverterRoute>"
    - from: "AppShell.kt"
      to: "NavigationBar"
      via: "Scaffold bottomBar parameter"
      pattern: "bottomBar.*NavigationBar"
---

<objective>
Build the two-tab navigation shell with Navigation3 and Material 3 NavigationBar, plus placeholder screens for Calculator and Converter. Wire AppShell into the existing MainActivity.

Purpose: Complete the app shell so users can launch the app and switch between tabs — the APP-02 requirement.
Output: Functional two-tab app with bottom navigation, placeholder content on each tab, Material 3 theming applied throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/phases/02-app-shell/02-RESEARCH.md
@.planning/phases/02-app-shell/02-01-SUMMARY.md

@app/src/main/kotlin/com/acalc/ui/MainActivity.kt
@app/src/main/kotlin/com/acalc/ui/AppTheme.kt
@app/build.gradle.kts

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From app/src/main/kotlin/com/acalc/ui/AppTheme.kt:
```kotlin
package com.acalc.ui

@Composable
fun AcalcTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    dynamicColor: Boolean = true,
    content: @Composable () -> Unit
)
```

From app/src/main/kotlin/com/acalc/ui/MainActivity.kt:
```kotlin
package com.acalc.ui

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            AcalcTheme {
                // AppShell will be added in Plan 02
            }
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder screens and AppShell with Navigation3 tab navigation</name>
  <files>
    app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt,
    app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt,
    app/src/main/kotlin/com/acalc/ui/AppShell.kt
  </files>
  <read_first>
    .planning/phases/02-app-shell/02-RESEARCH.md,
    app/src/main/kotlin/com/acalc/ui/AppTheme.kt,
    app/src/main/kotlin/com/acalc/ui/MainActivity.kt,
    app/build.gradle.kts
  </read_first>
  <action>
Create three files:

1. Create `app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt` (per D-07):
```kotlin
package com.acalc.ui.screens

import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.size
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Calculate
import androidx.compose.material3.Icon
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun CalculatorScreen(modifier: Modifier = Modifier) {
    Box(
        modifier = modifier.fillMaxSize(),
        contentAlignment = Alignment.Center
    ) {
        Column(horizontalAlignment = Alignment.CenterHorizontally) {
            Icon(
                imageVector = Icons.Default.Calculate,
                contentDescription = null,
                modifier = Modifier.size(64.dp)
            )
            Spacer(Modifier.height(16.dp))
            Text("Calculator", style = MaterialTheme.typography.headlineMedium)
        }
    }
}
```

Note on icons: `Icons.Default.Calculate` may not be in the default icon set. If it causes a compilation error, fall back to `Icons.Default.Menu` or another icon from the core set (`Icons.Default.*`). The extended icon set (`material-icons-extended`) should NOT be added as a dependency — it adds ~5MB. An alternative is to use `Icons.Default.Star` or `Icons.Default.Home` as a simple placeholder. Per Claude's discretion on icon choices, any recognizable icon is acceptable for placeholders.

If `Icons.Default.Calculate` is not available, use this fallback approach — create a simple vector icon inline or use `Icons.Default.Home` for Calculator and `Icons.Default.SwapHoriz` for Converter.

2. Create `app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt` (per D-08):
```kotlin
package com.acalc.ui.screens

import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.size
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.SwapHoriz
import androidx.compose.material3.Icon
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun ConverterScreen(modifier: Modifier = Modifier) {
    Box(
        modifier = modifier.fillMaxSize(),
        contentAlignment = Alignment.Center
    ) {
        Column(horizontalAlignment = Alignment.CenterHorizontally) {
            Icon(
                imageVector = Icons.Default.SwapHoriz,
                contentDescription = null,
                modifier = Modifier.size(64.dp)
            )
            Spacer(Modifier.height(16.dp))
            Text("Unit Converter", style = MaterialTheme.typography.headlineMedium)
        }
    }
}
```

Same icon fallback note applies — if `Icons.Default.SwapHoriz` is not available, use `Icons.Default.Refresh` or another core icon.

3. Create `app/src/main/kotlin/com/acalc/ui/AppShell.kt` (per D-01, D-02, D-03):

This is the core navigation file using Navigation3 1.0.1 APIs (NOT Navigation2).

```kotlin
package com.acalc.ui

import androidx.compose.foundation.layout.padding
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Calculate
import androidx.compose.material.icons.filled.SwapHoriz
import androidx.compose.material3.Icon
import androidx.compose.material3.NavigationBar
import androidx.compose.material3.NavigationBarItem
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation3.runtime.entry
import androidx.navigation3.runtime.entryProvider
import androidx.navigation3.runtime.rememberNavBackStack
import androidx.navigation3.ui.NavDisplay
import com.acalc.ui.screens.CalculatorScreen
import com.acalc.ui.screens.ConverterScreen

// Route keys for Navigation3 back stack (per D-03)
// These are simple objects used as keys — Navigation3 uses Any type
sealed interface TabRoute
data object CalculatorRoute : TabRoute
data object ConverterRoute : TabRoute

@Composable
fun AppShell() {
    val backStack = rememberNavBackStack(CalculatorRoute)
    val currentRoute = backStack.last()

    Scaffold(
        bottomBar = {
            NavigationBar {
                // Calculator tab (per D-01, D-02)
                NavigationBarItem(
                    selected = currentRoute is CalculatorRoute,
                    onClick = {
                        if (currentRoute !is CalculatorRoute) {
                            backStack.clear()
                            backStack.add(CalculatorRoute)
                        }
                    },
                    icon = { Icon(Icons.Default.Calculate, contentDescription = "Calculator") },
                    label = { Text("Calculator") }
                )
                // Converter tab (per D-01, D-02)
                NavigationBarItem(
                    selected = currentRoute is ConverterRoute,
                    onClick = {
                        if (currentRoute !is ConverterRoute) {
                            backStack.clear()
                            backStack.add(ConverterRoute)
                        }
                    },
                    icon = { Icon(Icons.Default.SwapHoriz, contentDescription = "Converter") },
                    label = { Text("Converter") }
                )
            }
        }
    ) { innerPadding ->
        NavDisplay(
            backStack = backStack,
            onBack = {},
            entryProvider = entryProvider {
                entry<CalculatorRoute> {
                    CalculatorScreen(modifier = Modifier.padding(innerPadding))
                }
                entry<ConverterRoute> {
                    ConverterScreen(modifier = Modifier.padding(innerPadding))
                }
            }
        )
    }
}
```

CRITICAL Navigation3 notes:
- Import from `androidx.navigation3.runtime` and `androidx.navigation3.ui` — NOT from `androidx.navigation.compose` (that is Navigation2)
- `rememberNavBackStack` takes an initial key (CalculatorRoute), NOT a string route
- `entryProvider { entry<K> { ... } }` is the Nav3 pattern — NOT `NavHost { composable("route") { } }` from Nav2
- `NavDisplay` takes `backStack`, `onBack`, and `entryProvider` — no `navController` parameter
- `onBack = {}` is appropriate for top-level tabs with no sub-navigation
- `innerPadding` is passed INTO screen composables via their `modifier` parameter — do NOT apply it to NavDisplay itself
- Tab switching clears the back stack and adds the new route — this is the correct pattern for top-level tabs with no sub-navigation within them
- Icon fallback: if `Icons.Default.Calculate` or `Icons.Default.SwapHoriz` are not in the core icon set and cause compilation errors, replace with any available `Icons.Default.*` icon (e.g., `Icons.Default.Home`, `Icons.Default.Refresh`)
  </action>
  <verify>
    <automated>cd /home/sanya/Desktop/ACALC && test -f app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt && test -f app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt && test -f app/src/main/kotlin/com/acalc/ui/AppShell.kt && grep -q "CalculatorRoute" app/src/main/kotlin/com/acalc/ui/AppShell.kt && grep -q "ConverterRoute" app/src/main/kotlin/com/acalc/ui/AppShell.kt && grep -q "NavigationBar" app/src/main/kotlin/com/acalc/ui/AppShell.kt && grep -q "NavDisplay" app/src/main/kotlin/com/acalc/ui/AppShell.kt && grep -q "rememberNavBackStack" app/src/main/kotlin/com/acalc/ui/AppShell.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/src/main/kotlin/com/acalc/ui/screens/CalculatorScreen.kt contains `fun CalculatorScreen(modifier: Modifier`
    - CalculatorScreen.kt contains `Text("Calculator"` (per D-07)
    - app/src/main/kotlin/com/acalc/ui/screens/ConverterScreen.kt contains `fun ConverterScreen(modifier: Modifier`
    - ConverterScreen.kt contains `Text("Unit Converter"` (per D-08)
    - app/src/main/kotlin/com/acalc/ui/AppShell.kt contains `fun AppShell()`
    - AppShell.kt contains `rememberNavBackStack(CalculatorRoute)` — Navigation3 API, NOT NavController
    - AppShell.kt contains `NavDisplay(` — Navigation3 display composable
    - AppShell.kt contains `entryProvider {` — Navigation3 entry provider
    - AppShell.kt contains `entry<CalculatorRoute>` and `entry<ConverterRoute>`
    - AppShell.kt contains `NavigationBar {` with two `NavigationBarItem` calls (per D-01)
    - AppShell.kt contains `selected = currentRoute is CalculatorRoute` and `selected = currentRoute is ConverterRoute` (per D-02)
    - AppShell.kt contains `Modifier.padding(innerPadding)` passed to screen composables
    - AppShell.kt does NOT contain `NavController`, `NavHost`, or `composable(` (Navigation2 APIs)
    - AppShell.kt imports from `androidx.navigation3` NOT from `androidx.navigation.compose`
  </acceptance_criteria>
  <done>Two placeholder screens and AppShell with Navigation3 tab navigation created. NavigationBar has two tabs with icons and labels. NavDisplay renders the correct screen per active route. innerPadding properly propagated to screen content.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppShell into MainActivity and verify full build</name>
  <files>
    app/src/main/kotlin/com/acalc/ui/MainActivity.kt
  </files>
  <read_first>
    app/src/main/kotlin/com/acalc/ui/MainActivity.kt,
    app/src/main/kotlin/com/acalc/ui/AppShell.kt
  </read_first>
  <action>
Update `app/src/main/kotlin/com/acalc/ui/MainActivity.kt` to call `AppShell()` inside `AcalcTheme`.

The updated MainActivity should be:
```kotlin
package com.acalc.ui

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            AcalcTheme {
                AppShell()
            }
        }
    }
}
```

The only change from Plan 01's version is adding `AppShell()` inside the `AcalcTheme { }` block.

After updating, run the full debug build: `./gradlew :app:assembleDebug`

If the build fails due to icon imports (e.g., `Icons.Default.Calculate` not found), fix by:
1. Check which icons are available in the default set — `Icons.Default` (also known as `Icons.Filled`) includes common icons
2. Replace unavailable icons with available alternatives from the core set
3. Do NOT add `material-icons-extended` dependency

If the build fails due to Navigation3 import issues, verify:
- `rememberNavBackStack` is imported from `androidx.navigation3.runtime`
- `NavDisplay` is imported from `androidx.navigation3.ui`
- `entry` and `entryProvider` are imported from `androidx.navigation3.runtime`
- The route objects `CalculatorRoute` and `ConverterRoute` are `data object` (not `object`) if serialization is required by Nav3

Run `./gradlew :app:testDebugUnitTest` to verify existing Phase 1 unit tests still pass (no regressions).
  </action>
  <verify>
    <automated>cd /home/sanya/Desktop/ACALC && ./gradlew :app:assembleDebug 2>&1 | tail -5 && ./gradlew :app:testDebugUnitTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MainActivity.kt contains `AppShell()` inside the `AcalcTheme {` block
    - MainActivity.kt still contains `enableEdgeToEdge()` before `setContent`
    - `./gradlew :app:assembleDebug` exits with `BUILD SUCCESSFUL`
    - `./gradlew :app:testDebugUnitTest` exits with `BUILD SUCCESSFUL` (no regressions in Phase 1 tests)
    - `app/build/outputs/apk/debug/app-debug.apk` exists after build
  </acceptance_criteria>
  <done>AppShell wired into MainActivity. App compiles, produces debug APK, and all existing unit tests pass. Full app shell is complete: Material 3 theming, edge-to-edge, two-tab bottom navigation with placeholder screens.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` succeeds — full app compiles with Navigation3, Material 3 theming, and placeholder screens
- `./gradlew :app:testDebugUnitTest` succeeds — no regressions in Phase 1 domain tests
- `app/build/outputs/apk/debug/app-debug.apk` exists
- AppShell.kt uses Navigation3 APIs only (no Nav2 imports)
- NavigationBar has exactly 2 NavigationBarItem entries
- Both placeholder screens accept a Modifier parameter and apply it
</verification>

<success_criteria>
- Bottom navigation bar with Calculator and Converter tabs renders correctly
- Tapping each tab switches the displayed screen content via Navigation3
- Active tab has visual selected state (Material 3 built-in indicator)
- Placeholder screens show centered icon + text as specified in D-07 and D-08
- innerPadding from Scaffold propagated to screen content (not overlapping nav bar)
- APP-02 requirement is complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-02-SUMMARY.md`
</output>
